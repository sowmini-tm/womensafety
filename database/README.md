# Database setup

This folder contains MySQL schema and seed plan for future phases.

## Local setup

1. Install MySQL.
2. Create the database:

```powershell
mysql -u root -p -e "CREATE DATABASE women_security_db;"
```

3. Update backend `.env` if needed.
